---
- hosts: all
  sudo: yes
  gather_facts: yes

  roles:
    - geerlingguy.packer-debian

  tasks:
    - apt: "pkg={{ item }} state=installed"
      with_items:
        - git
        - wget
        - vim